services:
  - type: web
    name: techbridge-frontend
    env: static
    buildCommand: echo 'Using pre-built files'
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
